{
  "name": "may_the_force",
  "version": "1.0.0",
  "description": "Populate a webpage with canonical Star Wars information.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "start": "server.js",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.1"
  }
}
